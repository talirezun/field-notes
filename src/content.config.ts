/**
 * Astro looks for the content config here. The actual schema lives next to the
 * content it validates, in content/config.ts, per the repo layout in the spec.
 */
export { collections } from '../content/config';
